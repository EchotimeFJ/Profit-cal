- [ ] 新增分钟级快照表迁移只创建新表，不修改、不删除、不覆盖旧数据
- [ ] 每分钟采集脚本可非交互执行，并为所有用户写入当前分钟快照
- [ ] 采集逻辑对同一用户、同一分钟、同一币种幂等，不产生重复记录
- [ ] 价格获取失败时使用现有成本基线 fallback，且不影响其他用户采集
- [x] `GET /api/portfolio/history?range=1d|3d|7d` 返回当前用户分钟级点位，按时间升序
- [x] 非法 range 返回 400，且历史查询接口不写数据库
- [ ] 前端切换 `1日/3日/7日` 会请求后端对应 range 并更新图表
- [ ] 图表保留金额纵轴和时间横轴，空数据/单点数据展示稳定
- [ ] 前端不再展示“基于手动刷新生成的每日快照，历史查询不会写入数据”
- [ ] CI 包含新增测试、脚本和服务的 unittest/compileall 检查
- [ ] 完整后端回归、前端构建、`git diff --check` 均通过
- [ ] 部署说明包含每分钟调度采集脚本的方法
